---
title: "ДЗ №2 — проверяемый чек-лист сдачи"
role: "QA"
deadline: "суббота, 29 августа 2026, 18:00"
sources:
  - "04 · Домашнее задание №2.md"
  - "05 · Материалы к ДЗ №2.md"
---

# 0. Как читать этот файл

Каждый пункт — чекбокс + строка **«Чем закрывается»**: конкретный артефакт в репозитории,
по которому проверяющий увидит выполнение. Если артефакта нет — пункт не закрыт,
как бы хорошо он ни был описан словами.

---

# ЛИЧНЫЕ РИСКИ — персональный блок

Основано на разборе прошлой работы по этому курсу. Это не требования задания,
а места, где я уже терял(а) баллы. Приоритет у этого блока выше, чем у общих
рекомендаций ниже по файлу.

## Р1. Коммиты: не меньше 8 за день, по одному в конце каждого смыслового шага

- **Что было в прошлый раз:** два коммита на девять сессий. Формулировка минуса:
  «41 файл доказательств появляется в истории одномоментно, хотя собирались они
  по шагам». История не показала работу — она показала один вброс.
- **Почему сегодня риск выше, а не ниже:** однодневный темп подталкивает
  «закоммичу всё в конце». Именно так прошлый минус и воспроизведётся, только быстрее.
- **Цель:** **≥ 8 коммитов за день**, каждый — в конце смыслового шага
  (спецификация готова → коммит; область проверок прогнана → коммит; отчёт о прогоне
  дописан → коммит; workflow сохранён → коммит; PDF собран → коммит и т. д.).
- **Правило шага:** шаг не считается завершённым, пока не сделан коммит. Не «сделаю
  потом», а прямо здесь.
- **Обязанность агента:** напоминать о коммите самостоятельно — в конце каждого
  смыслового шага, не дожидаясь просьбы. Если шаг закрыт, а коммита нет — сказать
  об этом до перехода к следующему шагу.
- [x] **Чем закрывается:** `git log --oneline` — не менее 8 строк с осмысленными  ← **9 коммитов, `git log --oneline`, сессия 1**
  сообщениями, разнесёнными по ходу дня, а не подряд в последний час.

## Р2. Одна задача — одна сессия

- **Что было в прошлый раз:** правило размылось изнутри — в сессию была дописана
  фраза «переходим к следующему шагу в этой же сессии». Итог: самый нагруженный этап
  оказался в самой перегруженной сессии, и единственный технический тупик случился
  именно там.
- **Правило:** новый смысловой шаг — новая сессия. Особенно это касается независимой
  проверки: агент-скептик, ломающий чужие чек-листы, всегда отдельной сессией
  («автор не бывает независимым проверяющим»).
- **Триггер, на который агент обязан среагировать:** фразы вида **«давай продолжим
  здесь»**, «остаёмся в этой сессии», «доделаем тут же». Прежде чем соглашаться —
  напомнить про этот пункт и про прошлый тупик, и только после подтверждения
  продолжать.
- [ ] **Чем закрывается:** `sessions/` — отдельный файл на каждый смысловой шаг;
  ни один файл не содержит двух разнородных этапов.

## Р3. Объём не сокращаем — сокращаем непрогнанное

- **Уточнение, отменяющее общий совет «взять поменьше»:** в прошлой работе было
  **66 проверок соответствия спецификации** и **полнота сдачи 15 из 15**. Количество
  кейсов — не риск и не проблема.
- **Риск в другом:** прогнаны кейсы или нет.
- **Правило набора:** **кейс, который не прогнан и не имеет зафиксированного
  результата, в набор не входит.** Не «прогоним позже», не «очевидно, что пройдёт» —
  такой кейс просто не является частью сдаваемого набора.
- **Следствие:** объём набора равен объёму прогона. Растёт прогон — растёт набор.
- [ ] **Чем закрывается:** у каждого кейса в наборе есть зафиксированный результат
  прогона (прошёл / упал / заблокирован) с датой и ссылкой на доказательство
  в `evidence/`; кейсов без результата в наборе нет.

## Р4. Промпт фиксируется в журнале до выполнения, а не после

- **Что было:** три промпта подряд выполнены, журнала сессий не существовало. Дословность
  задним числом восстанавливается только потому, что переписка ещё под рукой; через
  несколько сессий это будет невозможно.
- **Правило:** любой промпт, который меняет проект или запускает команды, **сначала**
  дословно записывается в текущий `sessions/session-N.md`, и только потом выполняется.
- **Жёсткое условие:** промпт не записан — действие не выполняется.
- **Дословно означает дословно:** исходные переносы строк, тире, опечатки. Не пересказ,
  не причёсанная формулировка, не «суть промпта».
- [~] **Чем закрывается:** в `sessions/` порядок записей совпадает с порядком действий;  ← **соблюдается с промпта 4; промпты 1–3 записаны задним числом**
  у каждого выполненного шага есть предшествующий ему записанный промпт.

## Р5. Варианты подаются без рекомендации

- **Правило:** когда есть выбор, я получаю плюсы и минусы каждого варианта, и решение
  остаётся за мной. Пометок «рекомендую», «лучше так», «(Recommended)» быть не должно.
- **Почему:** рекомендация свёртывает выбор до одной строки и прячет основание, по которому
  выбор делается. На разборе работы отвечать за выбор мне, а не агенту.
- [x] **Чем закрывается:** в местах развилок в `REPORT.md` и `sessions/` зафиксированы  ← **развилки зафиксированы в `REPORT.md` и `sessions/STATE.md`**
  варианты с плюсами и минусами и записано, какой выбран и почему.

---

# 1. Три обязательных пункта

## 1.1. Работающий результат

- [~] **Работающий результат есть и запускается по инструкции**  ← **125 проверок собраны, ожидания записаны до прогона; ПРОГОНА НЕТ, отчёта о прогоне нет — по риску Р3 ни один кейс пока не в наборе**
  - Формулировка задания: «на выходе — работающая вещь, а не описание»; размер намеренно
    небольшой — одна фича / одно исследование, доведённое до конца.
  - **Что считается результатом для QA:** *работающий набор проверок, который прогоняется
    и даёт результат* + *отчёт о прогоне*. Не тест-план в вакууме, а прогоняемый набор
    и зафиксированный итог прогона.
  - Приоритет — свой проект или своя рабочая задача; варианты из Приложения А (для QA
    ближе всего A4 «Тест-план платёжных сценариев», B4 «Регресс кредитного калькулятора»,
    B7 «Сверка выписок») — опора, если своего нет.
  - Ограничение для QA/нетехнических ролей: **код писать не нужно**, всё делается
    на текстовом проекте — папка с git, чек-листы, workflow.
  - **Чем закрывается:** репозиторий с набором проверок (чек-листы / сценарии / скрипт
    прогона) + файл отчёта о прогоне (`run-report.md` или раздел в `REPORT.md`)
    + `README.md` с инструкцией «как прогнать».

## 1.2. Агенты: запустить и попробовать

- [x] **Работа разбита на части и отдана субагентам параллельно, в одной сессии**  ← **сессия 2: агенты A/B/C одним блоком, пересечение интервалов 10 мин 05 с, три отдельных файла `tests/plan-*.md`; плюс агент D — независимый аудит**
  - Ориентир: три-четыре независимых исполнителя под одной задачей, каждому своя зона,
    чужую трогать запрещено. Для QA естественное деление — три области проверок.
  - Субагенты в Claude Code и Codex работают из коробки — достаточно попросить словами.
  - **Чем закрывается:** видно из журнала сессий / скриншотов / текста в `REPORT.md`,
    что работали именно субагенты; плюс сами артефакты, произведённые разными агентами
    (три отдельных файла по трём областям — самое наглядное доказательство).

- [~] *(необязательно, в плюс)* **Замер: последовательно против параллельно**  ← **параллельная половина замерена (`REPORT.md`, раздел «Замер»); последовательная НЕ делалась — решение заказчика, цена второго прогона под лимитами**
  - Замерять время и заполнение контекста главной сессии.
  - **Чем закрывается:** таблица «время / контекст главной сессии / качество» в `REPORT.md`.

## 1.3. Workflow — ОБЯЗАТЕЛЬНО

- [ ] **Собран минимум один workflow, применимый к реальной задаче**
  - Формат свободный: скилл, хук, workflow-файл, сохранённый промпт.
  - Пример для QA прямо из материалов: «взять изменения с последнего прогона → собрать
    список затронутых сценариев → прогнать → отчёт».
  - **Доказывать повторяемость не нужно** — достаточно приложить файл или текст.
  - **Чем закрывается:** файл в репозитории (`workflow/qa-run.md`, `.claude/skills/…`,
    хук — что угодно) + строка в `README.md`, как его вызывать.

---

# 2. Состав сдачи — все семь пунктов

- [ ] **1. Ссылка на репозиторий с работающим результатом** — *обязательно*
  - Публичный → просто ссылка. Приватный → доступ на чтение `@gfarhad93` + ссылка.
  - **Чем закрывается:** ссылка отправлена в телеграм `@oleg56x`.

- [x] **2. `README.md` — что сделано и как запустить** — *обязательно*  ← **`README.md` в корне; инструкции запуска приложения — в `app/README.md`**
  - Для команды — отдельно: кто что делал. Для своего варианта задания — одна фраза,
    почему выбрали своё.
  - **Чем закрывается:** `README.md` в корне репозитория.

- [ ] **3. Workflow — файл или текст** — *обязательно*
  - **Чем закрывается:** см. п. 1.3.

- [ ] **4. PDF — для текстовых результатов** — *обязательно*
  - QA с текстовым результатом (чек-листы, отчёт о прогоне) попадает сюда: сырой markdown
    и plain text не принимаются.
  - **Чем закрывается:** `.pdf` в репозитории (документ или презентация 8–12 слайдов 16:9).

- [x] **5. `REPORT.md` — по каждому пункту, который делали** — *по возможности*  ← **`REPORT.md`: 2 риска, 4 случая «что не сработало»**
  - Включая то, что **не** сработало.
  - **Чем закрывается:** `REPORT.md` в корне.

- [ ] **6. Замеры — последовательно против параллельно, эффект инструментов** — *необязательно*
  - **Чем закрывается:** таблицы с цифрами в `REPORT.md`.

- [x] **7. Журнал сессий `sessions/` — промпты дословно и решения** — *желательно, необязательно*  ← **`sessions/session-1.md`: 7 промптов дословно + `STATE.md`**
  - **Чем закрывается:** папка `sessions/` с файлами по сессиям.

---

# 3. Шесть критериев оценки

- [ ] **Работает** — результат запускается по инструкции, без «допишите тут своё».
  - **Чем закрывается:** прогон по `README.md` с нуля, ничего не дописывая; фиксация
    результата прогона.
- [ ] **Агенты запускались** — видно, что работа делалась субагентами, а не одним чатом.
  - **Чем закрывается:** журнал сессий + разделение артефактов по агентам.
- [ ] **Workflow есть** — приложен и применим к реальной задаче.
  - **Чем закрывается:** файл workflow + описание, к какой реальной процедуре он применим.
- [ ] **Оформление** — текстовый результат сдан как читаемый PDF.
  - **Чем закрывается:** PDF, просмотренный глазами (см. блок 6, «проверка перед сдачей»).
- [x] **Честность** — прямо сказано, что не сработало; отрицательный результат оценку  ← **4 случая в разделе «Что не сработало», включая 2 ошибки агента**
      **повышает**.
  - **Чем закрывается:** раздел «Что не сработало» в `REPORT.md` — минимум три честных
    тупика (в задании прямо сказано, что такой отчёт стоит дороже гладкого).
- [ ] **Плюсы** — команда · межролевая команда · worktrees · изоляция с доказательством ·
      инъекция на двух моделях · отчёт по инструментам · переход с архива на Git.
  - **Чем закрывается:** см. блок 4.

---

# 4. Необязательные пункты, которые дают плюс

- [ ] **Команда 3–4 человека** / **межролевая команда** (последнее — особенно ценится).
  - **Чем закрывается:** `README.md` с разделением ролей.
- [ ] **Git worktrees: три задачи одновременно, три ветки, затем merge.**  ← **не использовались: параллельность сделана субагентами в облачном контейнере, где `.git` им недоступен. Это соблюдение О-1, а не закрытие пункта про worktrees**
  - Для QA раскладка — три области проверок.
  - Убирать за собой: `git worktree remove`, `git worktree prune` (в эфире — случай
    с 250 ГБ).
  - **Чем закрывается:** история репозитория — три ветки, три набора коммитов, merge-коммиты;
    вывод `git worktree list` в отчёте.
- [~] **Изоляция с доказательством** — не «запускал в Docker», а **вывод неудачной попытки**  ← **отказ прокси на CONNECT снят и сохранён; попытка выхода через MCP не делалась**
      выйти за границу. Отдельно интересно: встроенная песочница Claude Code накрывает
      Bash и его дочерние процессы, но **не MCP** — попробовать дотянуться MCP-сервером.
  - **Чем закрывается:** скопированный вывод отказа (терминал) + результат попытки через MCP.
- [ ] **Недоверенный текст: инъекция на двух моделях, слабой и сильной.**
  - Фиксировать три исхода: *выполнила / промолчала / назвала вслух*. «Молча не сделала»
    и «сказала вслух» — разные уровни защиты.
  - Способы внедрения: комментарий, белый текст, метаданные, поле «назначение платежа»,
    «служебная строка».
  - **Чем закрывается:** заполненная таблица «способ × модель → выполнила / промолчала /
    назвала» + файлы-носители инъекции в репозитории.
- [ ] **Оптимизатор контекста и/или индекс кодовой базы** — с обязательной проверкой
      репозитория до установки (что ставит, какие хуки, куда ходит) и коротким честным
      отчётом в свободной форме.
  - **Чем закрывается:** раздел «Инструменты» в `REPORT.md`: что понравилось, что нет,
    что не сработало; при возможности — замер «один и тот же вопрос до и после».
- [ ] **Голосовой ввод** (Typeless / Wispr Flow / локальный Whisper).
  - **Чем закрывается:** пара строк в `REPORT.md`.
- [x] **Переход с архива на Git** — плюс отдельным пунктом для тех, кто в прошлый раз  ← **репозиторий с историей вместо архива**
      сдавал архивом.
  - **Чем закрывается:** сам факт репозитория с историей.

---

# 5. Срок и куда сдавать

- [ ] **Срок: до субботы, 29 августа 2026, 18:00.**
  - Больничный/отпуск — срок смещается, предупредить заранее. Не успели — принимают позже.
- [ ] **Куда:** ссылка на репозиторий — телеграм **`@oleg56x`**.
- [ ] **Приватный репозиторий:** доступ на чтение пользователю **`@gfarhad93`**.
- [ ] **Вопросы:** телеграм `@oleg56x` («уточняющий вопрос лучше молчаливого допущения»).

---

# 6. Требования, упомянутые вскользь, но проверяемые

Это то, что не вынесено в списки, но в тексте задания и материалов сформулировано
как условие приёмки. Здесь чаще всего теряются баллы.

- [ ] **Формат PDF — не «экспорт markdown», а собранный документ.**
  - «Сырой markdown и plain text не принимаются». Вёрстка собирается **тем же агентом**:
    HTML + CSS для печати → PDF, либо презентация (8–12 слайдов, 16:9, одна мысль на слайд).
  - Требования к документу: титульная страница (название, дата, автор), оглавление,
    резюме на полстраницы, разделы, таблицы вместо перечислений, один акцентный цвет,
    колонтитулы с номерами страниц, **никаких эмодзи и декоративного мусора**.
  - **Чем закрывается:** PDF в репозитории + отдельный проход «открой PDF и посмотри
    как читатель: где текст налезает, где страница пустая наполовину, где таблица
    не влезла» — и пересборка после правок.

- [ ] **Проверка на секреты перед сдачей.**
  - Проверять: `.env`, `settings.json`, история команд, скриншоты терминала.
  - Формулировка из эфира: «ключ утекает не через взлом, а через файл, который вы сами
    показали».
  - **Чем закрывается:** проход по репозиторию перед пушем (grep по ключам/токенам,
    просмотр всех скриншотов, `.gitignore`), отметка в `REPORT.md`, что проверка сделана.
  - Смежное: в задании A5 отдельным пунктом — *что из персональных данных нельзя класть
    в папку, которую читает агент*; если работаете с ПД (даже синтетическими) — сказать
    об этом явно.

- [x] **Дословность промптов в журнале сессий.**  ← **7 промптов дословно, с переносами и опечатками**
  - Требование именно «промпты **дословно** и решения» — не пересказ, не «попросил агента
    сделать проверки».
  - **Чем закрывается:** `sessions/` с копипастой промптов как есть, включая неудачные
    и переформулированные, + принятые по ходу решения.
  - Практический вывод: копировать промпты **по ходу работы**, задним числом дословность
    не восстанавливается.

- [x] **Раздел «что не сработало» — обязателен по духу, влияет на оценку.**  ← **раздел заведён, 4 случая**
  - «Отчёт с тремя честными тупиками стоит больше отчёта, где всё получилось с первого
    раза»; критерий «Честность» — отрицательный результат оценку **повышает**.
  - **Чем закрывается:** отдельный раздел в `REPORT.md` с конкретикой: что пробовали,
    что именно сломалось, чем закончилось.

- [ ] **Независимость проверяющего.**
  - «Автор не бывает независимым проверяющим» — агент-ломатель / скептик запускается
    **отдельной сессией**, не той, что делала работу. Для QA это прямо профильный приём:
    отдельный агент, которому поручено найти дыры в чужих чек-листах (см. A4).
  - **Чем закрывается:** отдельная сессия в `sessions/` + её находки в отчёте.

- [ ] **Проверка сторонних репозиториев до установки.**
  - Два запроса именно в таком порядке: сначала «что это, ничего не устанавливай»,
    потом «проверь с точки зрения безопасности → вердикт → если безопасен, установи».
  - Ориентир доверия: десятки тысяч звёзд и тысячи коммитов ≠ сотня звёзд. Цифрам
    из ролика и презентации не верить — смотреть страницу репозитория.
  - **Чем закрывается:** запись проверки в `sessions/` или `REPORT.md` (актуально, если
    ставите Superpowers, оптимизатор, индекс).

- [x] **Git как форма сдачи, а не папка.**  ← **9 коммитов по смысловым шагам, ветка `main`**
  - Ожидается видимая история: ветки, коммиты, журнал и результат в одном месте.
  - **Чем закрывается:** осмысленные коммиты по ходу работы, а не один «initial commit»
    за час до дедлайна.

- [ ] **`README.md` без «допишите тут своё».**
  - Критерий «Работает» сформулирован именно так — инструкция должна быть самодостаточной.
  - **Чем закрывается:** прогон инструкции с чистого листа, желательно чужими руками
    или отдельным агентом.

---

# 7. Финальный чек-лист перед отправкой (из материалов, раздел 11)

**Обязательно:**

- [ ] Результат запускается по инструкции из `README.md`
- [ ] Работа делалась субагентами — это видно
- [ ] Workflow приложен — файл или текст
- [ ] Текстовый результат оформлен в PDF
- [ ] Секретов в репозитории нет: `.env`, `settings.json`, история команд, скриншоты
- [ ] Для команды: в `README.md` написано, кто что делал

**Если делали — в плюс:**

- [ ] Замеры «последовательно против параллельно»
- [ ] Три worktree в истории, ветки смержены
- [ ] Изоляция доказана выводом неудачной попытки
- [ ] Инъекция проверена на двух моделях, таблица заполнена
- [ ] Отчёт по инструментам: что понравилось, что нет
- [x] Журнал сессий с дословными промптами  ← **`sessions/session-1.md`**
- [x] В прошлый раз сдавали архивом — в этот раз Git

---

# Сводка на конец сессии 1 — 25 августа 2026

Обозначения выше: `[x]` — закрыто, `[~]` — закрыто частично, `[ ]` — не начато.

## Закрыто

| Пункт | Чем |
|---|---|
| Р1 — не менее 8 коммитов | 9 коммитов по смысловым шагам |
| Р5 — варианты без рекомендации | Развилки с плюсами и минусами в `REPORT.md` и `STATE.md` |
| Состав сдачи, п. 2 — `README.md` | В корне; запуск приложения — в `app/README.md` |
| Состав сдачи, п. 5 — `REPORT.md` | 2 риска окружения, 4 случая «что не сработало» |
| Состав сдачи, п. 7 — журнал сессий | 7 промптов дословно + `STATE.md` |
| Критерий «Честность» | 4 случая, из них 2 — ошибки агента |
| Дословность промптов | С переносами и опечатками |
| Раздел «что не сработало» | Заведён, ведётся |
| Git вместо архива | История из 9 коммитов |

## Закрыто частично

| Пункт | Что есть | Чего не хватает |
|---|---|---|
| Р4 — промпт до выполнения | Соблюдается с промпта 4 | Промпты 1–3 записаны задним числом: журнала на тот момент не существовало |
| Изоляция с доказательством | Отказ прокси на `CONNECT`, вывод сохранён в `evidence/env-check.md` | Попытка пробить границу через MCP-сервер не делалась |

## Не закрыто — назвать вслух

**Обязательное:**

- **1.1 Работающий результат.** Приложение принято, но результат QA — это **набор
  проверок**, а его нет. Ни одной проверки не написано и не прогнано.
- **1.2 Агенты веером.** Субагент запускался один — автор приложения. Это не то, что
  требует задание: нужны три-четыре параллельных исполнителя под одной задачей.
  Запланировано на сессию 2.
- **1.3 Workflow.** Не начат. Обязательный пункт задания.
- **Состав сдачи, п. 1** — ссылка на репозиторий. Репозиторий локальный, на GitHub
  не выложен, доступ `@gfarhad93` не выдан.
- **Состав сдачи, п. 4** — PDF. Не начат.
- **Критерий «Работает»** — в части набора проверок.
- **Критерий «Оформление»** — следует за PDF.
- **Проверка на секреты перед сдачей.** Механизм есть (`pre-commit` хук), но финальный
  проход по репозиторию перед отправкой не делался.
- **`README.md` без «допишите тут своё».** Корневой `README.md` содержит раздел
  «Состояние на 25 августа» вместо инструкции по запуску проверок — потому что проверок
  ещё нет. Требует переписывания в конце.
- **Срок и куда сдавать.** Не сдано. Срок — суббота, 29 августа, 18:00.

**Необязательное, в плюс:**

- Замеры «последовательно против параллельно» — не делались.
- Worktrees — не использовались. Естественное место — три области проверок в сессии 2.
- Инъекция на двух моделях — не делалась.
- Отчёт по инструментам (`TOOLS.md`) — файла нет.
- Голосовой ввод — не пробовался.
- Команда — работа соло, пункт неприменим.
- Проверка сторонних репозиториев до установки — неприменимо: ничего не ставилось,
  реестр пакетов недоступен.

## Оценка объёма оставшегося

Обязательных пунктов не закрыто три из трёх по существу: набор проверок, агенты веером,
workflow. Плюс PDF и выкладка репозитория. Это основной объём работы, и он весь впереди.
Сессия 1 закрыла подготовку основы — спецификацию, объект тестирования, механизмы
и правила работы.

---

# Сводка на конец сессии 2 — 26 августа 2026

Обозначения: `[x]` — закрыто, `[~]` — закрыто частично, `[ ]` — не начато.

## Закрыто в сессии 2

| Пункт | Чем |
|---|---|
| **1.2 Агенты веером** — обязательный | Три агента A/B/C параллельно одним блоком вызовов, зоны не пересекаются, три отдельных файла `tests/plan-*.md`. Четвёртый агент D с чистым контекстом — аудит. Доказательство: `evidence/subagents-run.md` — старты `18:21:23` / `18:21:53` / `18:22:26`, разброс 63 с, пересечение интервалов 10 мин 05 с, отношение суммы длительностей к настенному времени 2,09 |
| Р1 — не менее 8 коммитов | 10 коммитов на конец сессии 2 |
| Р4 — промпт до выполнения | В сессии 2 соблюдено **полностью**, оба промпта записаны до действия. В сессии 1 промпты 1–3 были записаны задним числом |
| Р5 — варианты без рекомендации | Соблюдено; сессия 2 развилок с выбором не порождала — заказчик задала зоны и состав сама |
| Критерий «Честность» | Добавлены: ошибка постановщика с путём `/home/claude/work`, потеря показания о финише агента B, арифметическая ошибка в устном отчёте, щель между зонами `SP-152`. Все — с разбором, а не упоминанием |

## Закрыто частично

| Пункт | Что есть | Чего не хватает |
|---|---|---|
| **1.1 Работающий результат** | 125 проверок в трёх планах, ожидания записаны ДО прогона, деньги до копейки, трассировка на `SP-NNN` | **Прогона нет.** По риску Р3 ни один кейс не входит в сдаточный набор. Нет `run-report.md`, нет `README.md` с инструкцией «как прогнать» |
| **Замер: последовательно против параллельно** *(необязательный)* | Параллельная половина замерена целиком: время, контекст, доля главной сессии 21,8 %, сопоставление с занятием | Последовательного прогона нет — **решение заказчика**, цена второго прогона под лимитами среды. Следствие: 2,09 — измеренная параллельность, не измеренное ускорение |
| Изоляция с доказательством *(необязательный)* | Отказ прокси на `CONNECT` в `evidence/env-check.md` | Попытка пробить границу через MCP-сервер не делалась |

## Не закрыто — назвать вслух

**Обязательное:**

- **1.3 Workflow.** Не начат. Обязательный пункт задания, и он остался единственным
  необязательно-обязательным, к которому вообще не притрагивались. Каталог `workflow/`
  в репозитории есть, содержимого нет.
- **1.1 Работающий результат** — в части прогона. Планы есть, прогон впереди.
- **Состав сдачи, п. 1** — ссылка на репозиторий. Локальный, на GitHub не выложен,
  доступ `@gfarhad93` не выдан.
- **Состав сдачи, п. 4** — PDF. Не начат.
- **Критерий «Работает»** — следует за прогоном.
- **Критерий «Оформление»** — следует за PDF.
- **Проверка на секреты перед сдачей.** Механизм есть, финальный проход не делался.
- **`README.md` без «допишите тут своё».** Корневой `README.md` по-прежнему описывает
  состояние, а не запуск проверок. Переписать после прогона.
- **Уборка перед сдачей.** `evidence/hook-demo.txt`, папка `_transfer/`, содержимое
  `.git/_trash/` — среда их не удаляет, в `.gitignore` внесены, но с диска убрать вручную.
- **Срок.** Суббота, 29 августа, 18:00. Не сдано.

**Необязательное, в плюс:**

- **Worktrees** — не использовались и, скорее всего, уже не будут: параллельность
  закрыта субагентами в облачном контейнере, где `.git` им физически недоступен.
  Это соблюдение О-1, а не закрытие пункта про worktrees. Пункт остаётся открытым честно.
- **Последовательный замер** — закрыт решением заказчика как не делаемый.
- Инъекция на двух моделях — не делалась.
- Отчёт по инструментам (`TOOLS.md`) — файла нет.
- Голосовой ввод — не пробовался.
- Команда — работа соло, пункт неприменим.

## Оценка объёма оставшегося

Из трёх обязательных пунктов **закрыт один** (агенты веером), **один наполовину**
(результат: планы есть, прогона нет), **один не начат** (workflow). Плюс PDF, выкладка,
уборка и переписывание `README.md`.

Главный риск оставшегося — не объём, а порядок: **прогон нельзя начинать до разбора
аудита.** В планах два неисполнимых кейса (`C-35`, `C-31`), которые на прогоне занесут
в отчёт несуществующий дефект, и одно требование (`SP-152`), не покрытое никем.
Прогнать сначала и разбираться потом — значит получить отчёт, которому нельзя верить.
Порядок работ зафиксирован в `sessions/STATE.md`, раздел 4.


---

# Сводка на конец сессии 3 — 28 августа 2026

Обозначения: `[x]` — закрыто, `[~]` — закрыто частично, `[ ]` — не начато.

## Закрыто в сессии 3

| Пункт | Чем |
|---|---|
| **1.1 Работающий результат** — обязательный | Набор из **135 проверок** прогнан против `app/`: **68 зелёных, 67 красных**. Запускается одной командой без предварительных шагов и без зависимостей. Сырые выводы целиком — `evidence/run-all-api.txt` (119 тестов, 56/63), `evidence/run-all-ui.txt` (16 тестов, 12/4), `evidence/run-C-api.txt`, плюс снятые в сессии 3 `run-A-api.txt` и `run-B-api.txt` |
| **Отчёт о прогоне** | `REPORT.md`, раздел «Прогон набора проверок»: итог по слоям и зонам, 17 различных расхождений зоны C с требованиями и статусом каждого, разграничение «дефект против заказа» и «расхождение с оракулом» |
| **Р3 — кейс без результата прогона в набор не входит** | Снят полностью: у всех 135 тестов результат снят машиной, ни один не переписан рукой |
| **Р1 — не менее 8 коммитов** | **23 коммита** в `main` на конец сессии 3, из них 12 сделаны в сессии 3, из них 3 — слияния |
| **Worktrees** *(необязательный, в плюс)* | Использованы по-настоящему: три worktree `../work-A`, `../work-B`, `../work-C` на ветках `qa-A`, `qa-B`, `qa-C`, слияние в `main` тремя `merge --no-ff`, вывод каждого целиком в `evidence/merge-log.txt`, граф ветвления в `evidence/branch-graph.txt`. Уборка сработала не до конца — случай Н-7 |
| **Состав сдачи, п. 1 — ссылка на репозиторий** | Репозиторий публичный: `https://github.com/Alina8175/splitpay-qa`. Остаётся проверить, что доступ проверяющему выдан |
| **Р5 — варианты без рекомендации** | Соблюдено. Правка чужой оснастки зоны B предъявлена как выбор: что будет, если взять, и что будет с цифрами, если не брать |
| **Критерий «Честность»** | Добавлены четыре разбора: Н-5 (обрыв моста и переезд), Н-6 (замки пережили архивацию), Н-7 (`prune` не убирает заблокированную запись), плюс признанный в журнале брак собственной оснастки зоны B и пустой файл `run-all-api-postmerge.txt`, оставленный как есть |

## Закрыто частично

| Пункт | Что есть | Чего не хватает |
|---|---|---|
| **README.md без «допишите тут своё»** | Раздел «Намеренно красные тесты» с итогом прогона, разбором причин и объявленными неоднозначностями | Файл по-прежнему начинается с «Состояние на 25 августа, 14:00 MSK» и описывает ход работы, а не запуск. Инструкции «как прогнать» в начале нет |
| **Р4 — промпт до выполнения** | Все семь промптов сессии 3 записаны дословно, задним числом ничего не сочинялось | **Два из семи записаны задним числом** — промпты 5 и 6, оба короткие и принятые за уточнения. Правило нарушено дважды, разбор — в `sessions/session-3.md`, «Проверка полноты журнала» |
| **Доказательство прогона после слияния** | Прогон снят в контейнере на дереве, идентичном `main` после трёх слияний | `evidence/run-all-api-postmerge.txt` содержит одну строку `stdout is not a tty` и прогона не содержит. Локально UI не прогоняется вовсе — Playwright только в контейнере |
| **Изоляция с доказательством** *(необязательный)* | Отказ прокси на `CONNECT` в `evidence/env-check.md`; отказ моста `502` разобран в Н-5 | Попытка пробить границу через MCP-сервер по-прежнему не делалась |

## Не закрыто — назвать вслух

**Обязательное:**

- **1.3 Workflow.** Не начат. Единственный обязательный пункт задания, к которому
  не притрагивались ни в одной из трёх сессий. Каталога `workflow/` в репозитории
  нет — по запрету 4 заготовки не создаются.
- **Состав сдачи, п. 4 — PDF.** Не начат.
- **Проверка на секреты перед сдачей.** Механизм есть (`.githooks/pre-commit`,
  доказательство отказа — `evidence/hook-refusal.txt`), сплошной финальный проход
  не делался ни разу.
- **Уборка перед сдачей.** С диска убрать: `evidence/hook-demo.txt`, `_transfer/`,
  содержимое `.git/_trash/`, а также оставшиеся от сессии 3 `_pending/`
  и `_do-git-steps.sh`. Все внесены в `.gitignore`, в репозиторий не входят.
- **Критерий «Оформление»** — следует за PDF и переписанным `README.md`.
- **Срок.** Суббота, 29 августа 2026, 18:00. Не сдано.

**Необязательное, в плюс:**

- **Последовательный замер** — закрыт решением заказчика как не делаемый: цена
  второго полного прогона под лимитами среды. Следствие записано честно —
  2,09 это измеренная параллельность, а не измеренное ускорение.
- **Инъекция на двух моделях** — не делалась.
- **Отчёт по инструментам (`TOOLS.md`)** — файла нет. Материал накопился:
  три разных окружения, отказ моста, отсутствие `device_bash`, различие наборов
  инструментов между сборками.
- **Голосовой ввод** — не пробовался.
- **Команда** — работа соло, пункт неприменим.

## Оценка объёма оставшегося

Из трёх обязательных пунктов **закрыты два**: работающий результат с прогоном
и агенты веером. **Не начат один** — workflow, и он же единственный обязательный
из оставшегося.

Всё прочее — оформление: PDF, переписанный `README.md`, уборка, финальная проверка
на секреты. Ни один из этих пунктов не зависит от другого, порядок между ними
свободный.

**Главный риск оставшегося — срок, а не объём.** До 29 августа 18:00 нужно закрыть
workflow с нуля и сделать оформление. Содержательная часть работы — спецификация,
объект тестирования, планы, аудит, прогон и отчёт — закончена.

---

# Сводка на конец сессии 4 — 28 августа 2026 (финальная)

Обозначения: `[x]` — закрыто, `[~]` — закрыто частично, `[ ]` — не начато.

## Закрыто в сессии 4

| Пункт | Чем |
|---|---|
| **1.3 Workflow — последний незакрытый обязательный** | `workflow/qa-regress`: шесть шагов, каждый оставляет файл-доказательство. Прогнан вживую от первого шага до тега: `evidence/run-2026-08-27/` — 47 изменённых файлов, все три зоны, 135 тестов, 68/67, вердикт независимой сверки «ЧИСТО». Плюс `07-tag-check.txt`: повторный вызов сразу после первого показал 0 изменений от тега. Описание и вызовы — `workflow/README.md`. Второй формат workflow — `.githooks/pre-commit`, разобран в `REPORT.md` |
| **Состав сдачи, п. 2 — `README.md`** | Переписан целиком под запуск: что за проект и почему свой, три зоны, как поднять приложение, как прогнать API одной командой и UI отдельно с оговоркой про Playwright, итог числами, намеренно красные тесты, что за рамками итерации |
| **Критерий «Результат запускается по инструкции»** | Обе команды из `README.md` проверены в контейнере в этой сессии. API: 119 тестов, 56/63, 22,5 с. UI после `npm link playwright`: 16 тестов, 12/4, 31,1 с. Совпало с прогоном 27 августа до отдельного теста |
| **Состав сдачи, п. 4 — PDF** | `run-report.pdf`, 14 страниц: титул, оглавление, резюме, объект и границы, тест-план по зонам, результаты, 17 находок с дословными доказательствами, что не проверено, выводы. Собран печатью из `run-report.html`, пересобирается `node workflow/build-pdf.mjs`. Вычитан постранично как читатель, вёрстка правлена дважды |
| **Отчёт по инструментам** *(необязательный, в плюс)* | `TOOLS.md`: Superpowers, GitNexus, context-mode — что, зачем, почему не встало. Отдельно — отказ от выдачи агенту токена GitHub с разбором четырёх обходов |
| **Изоляция доказана выводом** *(необязательный, в плюс)* | `REPORT.md`, раздел «Изоляция» + `evidence/isolation-probe.txt`. Отказ прокси на `CONNECT` и `E403` на npm — из `env-check.md`; свежая проба 28 августа показала, что граница за три дня сдвинулась, и это записано как есть |
| **Проверка на секреты перед сдачей** | `evidence/secrets-scan.md`: рабочее дерево и вся история (23 коммита, все ветки). Секретов нет. Найдены и закрыты две дыры в самой защите — `_pending/` и `_do-git-steps.sh` не были в `.gitignore`; защита материалов курса держалась на одном символе-разделителе |
| **Уборка перед сдачей** | `CLEANUP.md` — список мусора с порядком удаления. Удаление за владельцем: среда `rm` не даёт (Р-1) |
| **Разделы отчёта: «Что не сработало», «Что не сделано и почему», «Размышления»** | Все три на месте. «Что не сработало» — сводная таблица десяти эпизодов Н-1…Н-10, включая два новых: Н-8 (промпты задним числом) и Н-10 (агент отказался нарисовать вывод установки). Отдельной строкой — форма сдачи: репозиторий с историей, а не архив |
| **Р4 — промпт до выполнения** | В сессии 4 соблюдено полностью: промпт записан в `sessions/session-4.md` дословно |
| **Журнал сессий** | `sessions/session-4.md`: промпт дословно, итог, размышления |

## Закрыто частично

| Пункт | Что есть | Чего не хватает |
|---|---|---|
| **Доказательство прогона после слияния** | Прогон снят заново процедурой `qa-regress` 28 августа, числа совпали с прогоном 27 августа до отдельного теста | `evidence/run-all-api-postmerge.txt` по-прежнему содержит одну строку `stdout is not a tty`. Оставлен как есть и назван вслух в `REPORT.md` — решение, а не недосмотр |
| **Покрытие оракула** | 132 требования из 138 покрыты полноценно | `SP-152` покрыто частично — щель между зонами, найденная аудитом (D-01) |

## Не закрыто — назвать вслух

**Обязательное — не осталось ничего.** Все три обязательных пункта (1.1
работающий результат, 1.2 агенты, 1.3 workflow) закрыты, все семь пунктов
состава сдачи закрыты.

**Необязательное, в плюс:**

- **Независимая проверка чужого набора («ломатель»)** — не сделана. Требует
  веера субагентов, лимиты заказчика в финальной сессии исчерпаны. **Что именно
  осталось непроверенным** — раздел «Что не сделано и почему» в `REPORT.md`:
  классификация 67 красных по существу, полнота покрытия чужим взглядом,
  достаточность самих ожиданий, устойчивость `qa-regress` к враждебному входу.
- **Инъекция в промпт на двух моделях** — не сделана, то же основание.
  Непроверенным осталось: данные приложения как канал инъекции (произвольный
  текст пользователя доходит до задания независимому агенту), имя файла
  из `git diff` как канал, различие между моделями.
- **Последовательный замер против параллельного** — закрыт решением заказчика
  как не делаемый. `2,09` — измеренная параллельность, не измеренное ускорение.
- **Голосовой ввод** — не пробовался.
- **Команда: кто что делал** — работа соло, пункт неприменим.
- **Проверка сторонних репозиториев до установки** — неприменимо, ничего
  не ставилось.

## Финальный чек-лист из материалов, раздел 11

**Обязательно:**

- [x] Результат запускается по инструкции из `README.md` — обе команды проверены в этой сессии
- [x] Работа делалась субагентами — это видно: `evidence/subagents-run.md`, `plan-audit.md`, `run-2026-08-27/04-audit-verdict.md`
- [x] Workflow приложен — `workflow/qa-regress` плюс `.githooks/pre-commit`
- [x] Текстовый результат оформлен в PDF — `run-report.pdf`, 14 страниц
- [x] Секретов в репозитории нет — сплошная проверка дерева и истории, `evidence/secrets-scan.md`
- [ ] Для команды: кто что делал — неприменимо, работа соло

**Если делали — в плюс:**

- [~] Замеры «последовательно против параллельно» — параллельная половина замерена, последовательной нет по решению заказчика
- [x] Три worktree в истории, ветки смержены — `evidence/merge-log.txt`, `branch-graph.txt`
- [x] Изоляция доказана выводом неудачной попытки — `env-check.md`, `isolation-probe.txt`, раздел «Изоляция»
- [ ] Инъекция проверена на двух моделях — не делалась, названо вслух с перечнем непроверенного
- [x] Отчёт по инструментам — `TOOLS.md`
- [x] Журнал сессий с дословными промптами — `sessions/session-1.md` … `session-4.md`
- [x] В прошлый раз сдавали архивом — в этот раз Git: 23 коммита, три ветки, три `merge --no-ff`, тег `last-qa-run`

## Что остаётся за владельцем репозитория

Не сделано агентом не по забывчивости, а потому, что делается только с машины:

1. **Коммит и push** — git-часть выполняет владелец.
2. **Тег `last-qa-run`** — поставить на машине и отправить:
   `git push --force origin last-qa-run`. Тег, поставленный процедурой
   в контейнере, локален для контейнера.
3. **Уборка мусора** — `CLEANUP.md`. Среда на машине не даёт `rm`.
4. **Подтвердить, что материалы курса игнорируются:**
   `git check-ignore -v 04*.md 04*.pdf 05*.md 05*.pdf`. Из контейнера
   проверить было нечем — мост вернул имена в испорченной кодировке.
5. **Проверить, что доступ проверяющему выдан.**
6. **Срок:** суббота, 29 августа 2026, 18:00.
