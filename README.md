# SplitPay QA — домашнее задание №2

Роль: **QA**. Работа выполняется соло, за один день, 25 августа 2026.

## Что это за проект

Репозиторий — не приложение, а **набор проверок и доказательств прогона**. Приложение
здесь — объект тестирования, а не результат работы. Результат работы QA по условию
задания — работающий набор проверок, который прогоняется и даёт результат, плюс отчёт
о прогоне.

## Что тестируем

**Объект тестирования — веб-приложение SplitPay для совместного учёта общих расходов.**

Приложение собирается **отдельным субагентом с чистым контекстом**, по короткому заказу.
Этот субагент не видит `SPEC.md` и не видит переписку, в которой обсуждались проверки.

Это сделано намеренно: объект тестирования должен быть написан независимо от того, чем его
будут проверять. Иначе автор и проверяющий — один и тот же контекст, и проверки подтверждают
не поведение приложения, а собственные предположения.

Из этого следует рабочее правило, действующее весь день:

> **SplitPay тестируется как чужое приложение. Код не читаем и не правим.**
> Все выводы делаются по наблюдаемому поведению — через API и через интерфейс.

Найденные дефекты фиксируются в отчёте, а не исправляются.

## Почему свой проект, а не вариант из приложения к заданию

Свой сценарий — единственный, в котором удаётся развести автора и проверяющего по разным
контекстам: приложение пишется под мой заказ, но без доступа к моим проверкам. Готовые
варианты из приложения к заданию такой развязки не дают — там объект либо уже существует
и известен, либо описывается тем же контекстом, который его потом проверяет.

## Состав репозитория

| Путь | Что внутри |
|---|---|
| `SPEC.md` | Спецификация ожидаемого поведения SplitPay — основание для проверок |
| `app/` | Объект тестирования. Пишется отдельным агентом, нами не правится |
| `tests/api/` | Проверки на уровне API |
| `tests/ui/` | Проверки на уровне интерфейса |
| `evidence/` | Доказательства: выводы прогонов, снимки, проверка окружения |
| `sessions/` | Журнал сессий: промпты дословно и принятые решения |
| `workflow/` | Повторяемая процедура прогона проверок |
| `REPORT.md` | Отчёт: что сделано, что найдено, что не сработало |
| `TOOLS.md` | Отчёт по инструментам |
| `task-checklist.md` | Разбор требований задания и личных рисков — рабочий чек-лист |

Материалы курса в репозиторий не входят и внесены в `.gitignore`.

## Ограничения окружения

Проверены до начала работы, полный вывод — в `evidence/env-check.md`.

- **Реестр npm недоступен** (`E403` через исходящий прокси) — ни на локальной машине,
  ни в облачном контейнере. Ни один шаг не должен зависеть от `npm install`; приложение
  и проверки строятся на Node.js без внешних зависимостей.
- **Playwright доступен только в облачном контейнере** (1.56.0, Chromium предустановлен).
  На локальной машине его нет и поставить нельзя. UI-проверки идут через контейнер.

## Состояние на 25 августа, 14:00 MSK

Сделано: репозиторий, `.gitignore`, проверка окружения, рабочий чек-лист требований.

Дальше по порядку: спецификация → заказ приложения отдельному агенту → проверки →
прогон → отчёт → PDF.

Инструкции запуска приложения и запуска проверок появятся в этом файле вместе с самим
приложением и набором проверок — отдельными коммитами, а не заготовками.
