# Проверка тега: повторный вызов сразу после первого прогона

Смысл: убедиться, что шаг 6 действительно сдвинул точку отсчёта.
Каталог этого прогона снесён — он был бы пустым по существу; сохранён вывод.

# Контекст прогона qa-regress

дата (UTC)      : 2026-08-28 07:19:35
репозиторий     : /home/claude/repo
HEAD            : befa676 workflow: qa-regress
ветка           : main
node            : v22.22.2
git             : git version 2.43.0
снято мёртвых замков (Р-1): 0
playwright      : доступен, UI-слой прогоняется

точка сравнения — тег last-qa-run (befa676)

--- изменённых файлов: 0 ---

--------------------------------------------------------------------------------------------------------------

ЗАТРОНУТЫХ ЗОН НЕТ: изменения не касаются поведения приложения.
Прогон не нужен. Это результат, а не отказ процедуры.

Итог: изменений от тега нет, прогон не запускался. Процедура различает
«нечего прогонять» и «прогон не сделан» — это разные файлы и разный вывод.
