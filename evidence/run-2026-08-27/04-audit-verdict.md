# Вердикт независимой сверки — 2026-08-27

**Расхождений: 0**

| № | Что заявлено | Что фактически | Где смотреть |
|---|---|---|---|
| — | Числа `# tests`/`# pass`/`# fail`: api 119/56/63, ui 16/12/4, итого 135/68/67 (`03-counts.tsv`) | Итоговые строки в сырых выводах совпадают дословно (api: `# tests 119`, `# pass 56`, `# fail 63`; ui: `# tests 16`, `# pass 12`, `# fail 4`); также совпадает независимый подсчёт строк `^ok ` / `^not ok ` (api 56/63, ui 12/4) | `03-run-api.txt:1920-1923`, `03-run-ui.txt:187-190` |
| — | Сопоставление файл→зона в `02-zone-map.txt` | Проверены все 47 строк против правил `zones.map` (path-ключи и правило полноты для файлов без ключа); для каждой строки найдено правило, дающее ту же зону, что указана в `02-zone-map.txt`. Расхождений не найдено | `02-zone-map.txt` построчно ⇔ `workflow/zones.map` |
| — | Затронутые зоны A,B,C прогнаны | В `03-run-api.txt` присутствуют подтесты всех трёх зон (49 из plan-a.test.mjs, 32 из plan-b.test.mjs, 38 из plan-c.test.mjs — по префиксам ID и по путям в `location`/`stack` для упавших случаев); в `03-run-ui.txt` — все три зоны (A: A-42…A-46-NET, 6 тестов; B: B-32…B-37, 6 тестов; C: C-39…C-42, 4 теста), имена тестов дословно совпадают с `test(...)` в `tests/api/plan-{a,b,c}.test.mjs` и `tests/ui/plan-{a,b,c}.spec.mjs` | `03-run-api.txt` (Subtest-строки), `03-run-ui.txt:1-186` |
| — | Признаки обрыва прогона | В обоих файлах: один заголовок `TAP version 13`, одна строка плана (`1..119` / `1..16`), полный итоговый блок (`# tests`/`# pass`/`# fail`/`# cancelled`/`# skipped`/`# todo`/`# duration_ms`), код возврата 1 согласован с ненулевым `# fail`. Признаков падения оснастки (uncaughtException, MODULE_NOT_FOUND, обрыв до итогового блока) не найдено | `03-run-api.txt:1919-1928`, `03-run-ui.txt:186-195` |
| — | Пройденным заявлено то, что не прогонялось | `# skipped 0`, `# todo 0` в обоих файлах; сумма `ok`+`not ok` строк точно равна заявленному числу тестов в каждом слое (56+63=119, 12+4=16) | `03-run-api.txt:1924-1926`, `03-run-ui.txt:191-193` |

## Вывод
ЧИСТО
