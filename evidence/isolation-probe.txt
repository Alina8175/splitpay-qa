# Зонд границы изоляции — облачный контейнер сессии 4
# снято 2026-08-28 07:23:01 UTC, окружение: облачный контейнер
# Тестовых прогонов не выполнялось: это четыре сетевые пробы.

$ npm ping
npm notice PING https://registry.npmjs.org/
npm notice PONG 136ms
exit=0

$ curl -sS -o /dev/null -w "%{http_code}\n" https://registry.npmjs.org/
200
exit=0

$ curl -sS -o /dev/null -w "%{http_code}\n" https://github.com/Alina8175/splitpay-qa
403
exit=0

$ git ls-remote https://github.com/Alina8175/splitpay-qa HEAD
c834fcd56350616b895e61e6ad3f90299610b425	HEAD
exit=0

--- Что из этого следует -------------------------------------------------

Сравнение с evidence/env-check.md (снято 25 августа, то же семейство окружений):

| Проба | 25 августа, контейнер | 28 августа, контейнер |
|---|---|---|
| npm ping                  | E403 Forbidden | PONG 136ms |
| curl registry.npmjs.org   | 403            | 200 |
| curl github.com/<репо>    | не проверялось | 403 от прокси |
| git ls-remote github.com  | работал (clone в сессии 3) | работает |

Три наблюдения, которые из этого следуют:

1. Граница НЕ постоянна: политика исходящего трафика контейнера изменилась
   между сессиями. Утверждение об изоляции действительно только вместе
   с датой и названием окружения.
2. Граница проходит НЕ по хосту, а по протоколу и пути: github.com отвечает
   403 на обычный HTTP-запрос страницы и при этом обслуживает git по тому же
   адресу. Один и тот же хост одновременно закрыт и открыт.
3. Граница принадлежит окружению, а не работе. Локальная машина заказчика
   на 25 августа отклоняла ВСЁ на CONNECT; контейнер на 28 августа
   не отклоняет почти ничего. Свежей пробы с локальной машины в этой сессии
   нет: инструмента запуска команд на ней в этой сборке не существует
   (device_bash отсутствует), поэтому её строка взята из env-check.md
   и датирована 25 августа, а не выдана за сегодняшнюю.
