# Сессия 3

Формат: промпт фиксируется ДОСЛОВНО до начала выполнения (правило из промпта
сессии 3). Ниже — исходный текст без сокращений и без правок.

---

## Промпт 1 (дословно)

```
Роль: QA-тестировщик. Проект — домашнее задание №2, папка splitpay-qa,
репозиторий git, ветка main, 10 коммитов.
Прочитай sessions/STATE.md целиком, затем SPEC.md, tests/plan-A.md,
plan-B.md, plan-C.md и evidence/plan-audit.md. Переписку прошлых сессий
не восстанавливаем.
ПРАВИЛА
— Промпт фиксируешь ДОСЛОВНО в sessions/session-3.md до выполнения.
  Не записан — не выполняй.
— Не помечай варианты как «рекомендую»: плюсы и минусы, решаю я.
— Темп важнее полноты обсуждения. Решай сам там, где выбор обратим;
  спрашивай только если это меняет состав сдачи.
— Отчитывайся коротко, развёрнутое пиши в файлы.
ЗАДАЧА 1. РАЗБОР АУДИТА.
41 находка в evidence/plan-audit.md. Пройди все, по каждой реши: принять
и починить план, отклонить с обоснованием, или вынести в неоднозначности.
Отдельно закрой SP-152 — требование не покрыто никем, щель между зонами.
Отдельно почини C-35 и C-31 — кейсы неисполнимы и дали бы ложный дефект.
Результат — обновлённые tests/plan-*.md и таблица решений в
evidence/audit-resolution.md. Коммит.
ЗАДАЧА 2. ТЕСТЫ В ТРЁХ WORKTREE.
Три worktree с отдельными ветками, но работай в них ПО ОЧЕРЕДИ, а не
параллельно: лимиты среды дороже, а плюс за worktrees засчитывается
по истории веток и слияниям, не по одновременности. Помни ограничение
О-1: git-операции не пересекаются.
  ../work-A, ветка qa-A — тесты по tests/plan-A.md
  ../work-B, ветка qa-B — тесты по tests/plan-B.md
  ../work-C, ветка qa-C — тесты по tests/plan-C.md
Основной набор — встроенный node --test, без зависимостей, против HTTP-API.
Кладём в tests/api/. Он должен запускаться одной командой у проверяющего.
UI-слой — Playwright, в tests/ui/. Подключается офлайн командой
npm link playwright: пакет установлен глобально в контейнере, в реестр
она не ходит. Проверено. Селекторы — getByRole и getByText, не data-testid.
Требования к тестам:
— тест, нашедший дефект, остаётся КРАСНЫМ; xfail не использовать;
— денежные ожидания до копейки, не «примерно»;
— сырой вывод прогона целиком — в evidence/;
— app/ не править: находка фиксируется тестом, а не фиксом.
ЗАДАЧА 3. СЛИЯНИЕ И ПРОГОН.
Покажи diff каждой ветки, слей в main по одной с выводом каждого merge
целиком. Убери worktree; если удаление упрётся в среду — покажи отказ
как есть, это Р-1.
Прогони полный набор, покажи итог: сколько зелёных, сколько красных,
что именно красное и почему. Вывод целиком в evidence/.
В README отдельным разделом — намеренно красные тесты.
Раздел про worktrees в REPORT.md: что делали, что конфликтовало, где
применимо, что не отработало.
Коммит после каждой задачи.
```

---

## Ход работы

(заполняется по мере выполнения)

---

# Продолжение сессии 3 — 27 августа 2026

Сессия 3 оборвалась при переезде между машинами. Ниже — промпты продолжения,
дословно, в порядке поступления. Нумерация продолжает промпт 1 выше.

Промпты 2 и 3 записаны в рабочую папку агента до выполнения и перенесены сюда;
промпт 4 записан агентом напрямую в этот файл через мост до машины.

---

## Промпт 2 (дословно)

```
QA-тестировщик. Папка splitpay-qa, git, ветка main, 16 коммитов,
всё запушено на GitHub.
Прочитай sessions/STATE.md — там рамка, правила и запреты, соблюдай их.
Остальное открывай по необходимости. Промпт фиксируй дословно
в sessions/session-3.md до выполнения.
Контекст: сессия 3 оборвалась при переезде между машинами. Ветки qa-A
и qa-B с тестами зон A и B закоммичены. Ветка qa-C пуста — тесты зоны C
писались в контейнере и не сохранились. Папок worktree больше нет,
записи о них вычищены git worktree prune.
Доделай:
1. Тесты зоны C по tests/plan-C.md, в ветку qa-C. Файлы, которые я
   принесу из старой сессии, положи как есть; чего нет — напиши заново.
2. Три Playwright-спека по зонам A, B, C в tests/ui/.
3. Слей qa-A, qa-B, qa-C в main по одной, с выводом каждого merge целиком.
4. Прогони весь набор. Итог: сколько зелёных, сколько красных, что
   красное и почему. Вывод целиком в evidence/.
5. Разделы: намеренно красные тесты в README, worktrees в REPORT.md.
   В REPORT.md отдельно запиши обрыв моста и переезд между машинами
   как эпизод «что не сработало».
Коммит после каждого пункта, в конце git push.
```

**Не выполнен.** Мост до машины не поднялся: MCP-сервер `remote-devices` вернул
`502 Bad gateway`, состояние соединения — `failed`, проверено дважды, включая
принудительный `RefreshMcpTools`. Ни `sessions/STATE.md`, ни `tests/plan-C.md`,
ни `app/`, ни `.git` не были доступны. Разбор — случай Н-5 в `REPORT.md`.

---

## Промпт 3 (дословно)

```
Мост не нужен. Репозиторий теперь публичный на GitHub, и твой контейнер
до него дотягивается — я проверила, клонирование работает.
1. Склонируй его к себе в контейнер:
   git clone https://github.com/Alina8175/splitpay-qa.git
   Прочитай sessions/STATE.md — рамка, правила и запреты. Затем
   tests/plan-C.md и SPEC.md в части зоны C.
2. Напиши тесты зоны C по tests/plan-C.md — встроенный node --test,
   без зависимостей, против HTTP-API приложения из app/.
   Плюс три Playwright-спека по зонам A, B, C в tests/ui/ — Playwright
   у тебя предустановлен, подключается через npm link playwright,
   селекторы getByRole и getByText.
3. Прогони всё против app/ и сохрани сырые выводы целиком.
4. Отдай мне ФАЙЛАМИ В ЧАТ: тесты зоны C, три Playwright-спека, сырые
   выводы прогонов, готовые разделы для README (намеренно красные тесты)
   и REPORT.md. Плюс короткую записку: какой файл куда класть и какие
   git-команды выполнить.
Пушить не пытайся — авторизации у тебя нет, git-часть (коммиты, слияния,
push) я сделаю сама на своей машине.
Промпт запиши дословно себе в рабочую папку, я перенесу его
в sessions/session-3.md, когда буду коммитить.
```

**Выполнен.** Репозиторий склонирован с GitHub в облачный контейнер. Написаны
`tests/api/plan-c.test.mjs` (38 проверок) и `tests/ui/plan-c.spec.mjs` (4 кейса),
исправлена оснастка `tests/ui/plan-b.spec.mjs`, снят прогон всего набора:
135 тестов, 68 зелёных, 67 красных. Выводы — `evidence/run-all-api.txt`,
`evidence/run-all-ui.txt`, `evidence/run-C-api.txt`.

---

## Промпт 4 (дословно)

```
Проверь, поднялся ли мост: попробуй прочитать sessions/STATE.md
с моего компьютера.
Если получилось — сделай всё сам: разложи подготовленные файлы по местам
в репозитории, закоммить тесты зоны C в ветку qa-C, слей qa-A, qa-B, qa-C
в main по очереди с выводом каждого merge в evidence/, прогони весь набор,
сохрани вывод, допиши разделы в README и REPORT. Пушить не надо, я сама.
Если мост не работает — скажи прямо, тогда буду скачивать файлы руками.
```

**Выполнен частично.** Мост поднялся на чтение и запись файлов, и через него
разложены по местам: проверки зоны C, UI-спек зоны C, три файла сырых выводов,
разделы в `README.md` и `REPORT.md`, этот журнал. Git-часть (коммиты, слияния)
не выполнена: в этой сборке моста нет инструмента запуска команд на машине —
только чтение и запись файлов. Команды для выполнения вручную переданы отдельной
запиской.
